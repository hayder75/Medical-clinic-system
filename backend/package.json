{
  "name": "medical-clinic-backend",
  "version": "1.0.0",
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "scripts": {
    "migrate": "prisma migrate dev",
    "generate": "prisma generate",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker:start": "npx prisma db push && npx prisma generate && node seed-test-users.js && node server.js",
    "build": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "pdfmake": "^0.2.20",
    "roboto-font": "^0.1.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "nodemon": "^3.0.1"
  }
}

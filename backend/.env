# Database Configuration
# Update with your local PostgreSQL credentials
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/medical_clinic_system"

# JWT Secret (generate a random string for local development)
JWT_SECRET="your-local-jwt-secret-key-change-this"

# Node Environment
NODE_ENV="development"

# Server Port (optional, defaults to 3000)
PORT=3000
